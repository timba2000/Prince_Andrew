The user (Timothy) wants me to be **autonomous**.
- **Rule:** Run all tasks that don't breach safety controls.
- **Rule:** Ask ONLY for approval (don't ask "should I do this?" for standard tasks, just do them and report).
- **Action:** I have set up a local backup script inside the container at `/data/backup-openclaw.sh` scheduled for 3 AM daily.
- **Context:** The user has previously lost their agent data and fears a rebuild. Backups are critical priority.
